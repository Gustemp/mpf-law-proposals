import AgentNode from './AgentNode';
import TaskNode from './TaskNode';
import ToolNode from './ToolNode';

export const nodeTypes = {
  agent: AgentNode,
  task: TaskNode,
  tool: ToolNode,
};

export { AgentNode, TaskNode, ToolNode };
